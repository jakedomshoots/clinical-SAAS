{
  "name": "@concierge-os/api",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "lint": "ruff check app/ tests/",
    "typecheck": "mypy app/",
    "test": "pytest",
    "clean": "rm -rf __pycache__ .mypy_cache .pytest_cache"
  }
}
